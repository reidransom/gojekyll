## Agent skills

### Issue tracker

Issues are tracked as local Markdown files under `.scratch/`. See `.agents/issue-tracker.md`.

### Triage labels

Triage state uses the five canonical label strings. See `.agents/triage-labels.md`.

### Domain docs

This repository uses a single-context domain-doc layout. See `.agents/domain.md`.

### Documentation site

Instructions for working on the user-facing documentation site are in `.agents/docs-site.md`.
